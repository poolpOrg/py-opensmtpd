import os
from setuptools import setup

setup(name = "py-opensmtpd",
      version = "0.0.1",
      description = "py-opensmtpd is a python interface to OpenSMTPD APIs",
      author = "Gilles Chehade",
      author_email = "gilles@poolp.org",
      packages = [ "opensmtpd" ],
)
